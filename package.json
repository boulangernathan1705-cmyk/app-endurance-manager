{
  "name": "endurance-manager",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:workers": "node scripts/build.mjs --workers",
    "audit": "node scripts/audit-codebase.mjs",
    "audit:strict": "node scripts/audit-codebase.mjs --strict",
    "check": "npm test && npm run build:workers && npm run audit:strict",
    "db:migrations:apply": "npx wrangler d1 migrations apply DB --remote",
    "deploy:workers": "npm run db:migrations:apply && npx wrangler deploy",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
