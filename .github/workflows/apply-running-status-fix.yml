name: Apply running status fix
on:
  push:
    branches: [fix/running-status]
    paths:
      - 'scripts/apply-running-status-fix.mjs'
      - '.github/workflows/apply-running-status-fix.yml'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.13.0'
      - name: Apply changes
        run: node scripts/apply-running-status-fix.mjs
      - name: Run checks
        run: npm run check
      - name: Commit changes
        run: |
          rm scripts/apply-running-status-fix.mjs .github/workflows/apply-running-status-fix.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: simplify running event status' || exit 0
          git push
